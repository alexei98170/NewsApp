@model NewsApp.Models.News
@{
    ViewBag.Title = Model.Title;
}
<h2>Пользователь @Model.CreatedBy</h2>
<div>
    <dl class="dl-horizontal">
        <dt>Идентификатор</dt>
        <dd>@Model.Id</dd>
 
        <dt>Название</dt>
        <dd>@Model.Title</dd>
    </dl>
</div>